在 LaTeX 里，“字号”本质上就是**点数 pt**（更准确说 TeX point），所以你可以用两条路表示：

**A. 直接用 pt 指定字号（最精确，和 Word 的 5、5.5、6…72 一一对应）**
- 用法（推荐配合 `ctex`，最常用）：`{\zihao{-4} ...}` 或 `{\fontsize{<pt>}{<baselineskip>}\selectfont ...}`
- 任意 Word 点数 $x$：  
  - `{\fontsize{x}{1.2x}\selectfont 文字}`  
  - 例：`{\fontsize{12}{14.4}\selectfont 这是12pt}`
- 你提到的离散值都能直接写：  
  - `{\fontsize{5}{6}\selectfont ...}`  
  - `{\fontsize{5.5}{6.6}\selectfont ...}`  
  - `{\fontsize{18}{21.6}\selectfont ...}`  
  - `{\fontsize{72}{86.4}\selectfont ...}`  
说明：第二个参数是行距（baseline skip），常用经验值是 `1.2x`；如果学校要求“固定值 20pt”这种，就把第二个参数直接写成固定数。

**B. 用“中文字号名/号数”（更符合国内模板习惯，依赖 `ctex`）**
在中文模板里通常用 `ctex` 的 `\zihao{...}` 表示“初号/小初/一号…小五…八号”。

常用对照（约定值，单位 pt；不同体系可能有极小差异，但 `ctex` 基本按这套来）：
- 初号：42pt → `\zihao{0}`
- 小初：36pt → `\zihao{-0}`
- 一号：26pt → `\zihao{1}`
- 小一：24pt → `\zihao{-1}`
- 二号：22pt → `\zihao{2}`
- 小二：18pt → `\zihao{-2}`
- 三号：16pt → `\zihao{3}`
- 小三：15pt → `\zihao{-3}`
- 四号：14pt → `\zihao{4}`
- 小四：12pt → `\zihao{-4}`
- 五号：10.5pt → `\zihao{5}`
- 小五：9pt → `\zihao{-5}`
- 六号：7.5pt → `\zihao{6}`
- 小六：6.5pt → `\zihao{-6}`
- 七号：5.5pt → `\zihao{7}`
- 八号：5pt → `\zihao{8}`

**你问的两类需求怎么选**
- 你要“Word 里写 5、5.5、6…72 的那种点数精确复刻”：优先用 `\fontsize{...}{...}\selectfont`
- 你要“论文/报告规范里的初号、小四、五号”：用 `\zihao{...}` 更直观

**最小示例（你以后写模板时就这么用）**
- `{\zihao{-4} 小四正文}`
- `{\fontsize{10.5}{20}\selectfont 五号且固定20pt行距}`（举例：如果学校要求固定行距 20pt）

如果你把学校 Word 模板里“正文/标题1-5/图表/注释/页眉页脚”的字号和行距要求列出来，我可以直接帮你写成一段可放进 `.cls` 的统一设置（例如 `ctexset` 的标题格式 + 正文行距策略）。


新命令\Mainsection{},代替原本的\section{},这下面的文字(下一个\命令出现)使用五号,中文用宋体,英文用times new roman,首行缩进0.74cm,行距为单倍行距,调整不要出现单字一行,行首标点的情况,自动适应带有数学公式的换行,
大括号{}内的为二级标题,里面的中文使用小四黑,英文使用Arial,{}内的字,到上面的文字以及到下面的文字隔6pt